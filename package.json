{
  "name": "itcd-testes-automatizados",
  "version": "1.0.0",
  "description": "Automação de testes para o sistema ITCD - Sefaz MS",
  "main": "index.js",
  "scripts": {
    "cy:abrir": "npx cypress run --headed --browser chrome",
    "cy:executar": "cypress run",
    "cy:smoke": "cypress run --env tags=@smoke"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^21.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "cypress": "^13.15.0",
    "typescript": "^5.0.0",
    "esbuild": "^0.23.0"
  }
}